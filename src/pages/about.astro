---
import "../style/global.css";
const { pageTitle } = Astro.props;
const identity = {
  firstName: "Mario",
  country: "Tampico",
  occupation: "Practicante de Astro",
  hobbies: ["fotografia", "observación de aves", "Juagr videojuegos"],
  favoriteFood: "Tacos de barbacoa",
};
import Layouts from "../layouts/Layouts.astro";
const skills = [
  "MySQL",
  "C++",
  "Redes",
  "SQL-Server",
  "Redacción de documentos",
];
const happy = true;
const finished = false;
const goal = 1;
const skillColor = "Brown"; // Define the color of the skills
---

<html lang="es">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>{pageTitle}</title>
  </head>
  <body>
    <Layouts >
      <h1>{pageTitle}</h1>
      <h2>¡y mi nuevo sitio Astro!</h2>
      <p>
        ¡Hola! Soy un Practicante y estoy aprendiendo Astro. ¡Estoy emocionado de
        ver cómo funciona!
      </p>
      <p>
        Estoy trabajando en el tutorial introductorio de Astro. Esta es la segunda
      página de mi sitio web, ¡y es la primera que he construido yo mismo!
    </p>
    <p>
      Este sitio se irá actualizando a medida que vaya completando más partes
      del tutorial, ¡así que no dejes de visitarlo para ver cómo va mi viaje!
    </p>
    <p>Aquí tienes un poco más sobre mí:</p>
    <ul>
      <li>Nombre: {identity.firstName}</li>
      <li>País: {identity.country}</li>
      <li>Ocupación: {identity.occupation}</li>
      {
        identity.hobbies.length >= 2 && (
          <li>
            Dos de mis aficiones son: {identity.hobbies[0]} y{" "}
            {identity.hobbies[1]}
          </li>
        )
      }
      <li>Comida favorita: {identity.favoriteFood}</li>
    </ul>
    <p>Mis habilidades son:</p>
    <ul>
      {skills.map((skill) => <li class="skill"> {skill}</li>)}
    </ul>
    <p>
      {happy && <p>¡Estoy feliz de aprender Astro!</p>}
      
      {finished && <p>¡He terminado este tutorial!</p>}
      
      {
        goal === 1 ? (
          <p>Mi objetivo es terminar en 1 dia.</p>
        ) : (
          <p>Mi objetivo es terminar ya.</p>
        )
      }
    </p>
    </Layouts>
    <script>
      import "../scripts/menu.js";
    </script>
  </body>
</html>

<style define:vars={{ skillColor }}>
  body {
    font-family:
      Inter, Roboto, "Helvetica Neue", "Arial Nova", "Nimbus Sans", Arial,
      sans-serif;
  }
  h1 {
    color: purple;
    font-size: 4rem;
    margin-top: 0.25em;
  }
  .skill {
    color: var(--skillColor);
    font-weight: bold;
  }

  #links {
    display: flex;
    gap: 16px;
  }
  #links a {
    display: flex;
    align-items: center;
    padding: 10px 12px;
    color: #111827;
    text-decoration: none;
    transition: color 0.2s;
  }
  #links a:hover {
    color: rgb(78, 80, 86);
  }
</style>
